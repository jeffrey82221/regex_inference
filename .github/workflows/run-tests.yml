name: Run tests
on: 
  pull_request:
    types: [opened, synchronize]
    branches:
      - main
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Test Environment
        run: python -c 'import os;print(os.environ["OPENAI_API_KEY"])'
        env: 
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
      - name: run type checking
        run: | 
         pip install mypy
         pip install types-requests
         mypy regex_inference --ignore-missing-imports
      - name: flake8 checking
        run: |
         pip install flake8
         flake8 --ignore=E501,W504
      - name: Insert OpenAI API KEY
        run: export OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}
      - name: run tests
        run: | 
          pip install pytest
          pytest
      - name: check version
        run: |
          pip install requests
          python .github/check_version.py